import React, { Component } from 'react';

import { Button, Counter } from 'common';

export default class App extends Component {
  render() {
    return (
      <div>
        <Counter />
      </div>
    );
  }
}
